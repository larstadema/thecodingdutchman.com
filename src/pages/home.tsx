import React, { Fragment } from 'react';

import { Footer, Header, Main } from '../components';

const Home = () => (
  <Fragment>
    <Header />
    <Main />
    <Footer />
  </Fragment>
);

export default Home;
